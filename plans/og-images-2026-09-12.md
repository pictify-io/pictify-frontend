# OG images — regenerated in the new brand (2026-09-12)

~1,670 of 1,789 monthly clicks were landing on pages whose share card was still the old cream/orange
brand. Every page card is now the field-green Repro Shop card (Paper board `BIM-0`).

## What was generated

`scripts/og-v2.py` is still the only generator. It gained three things rather than a sibling script:

- `tool:<slug>` — reads `src/lib/pseo/tool-cards.js` through node, so a new registry entry gets a card
  with no edit here. Eyebrow `FREE TOOL · <meta>`, headline the registry title, sub the registry `desc`.
- `alt:<slug>` — reads `src/lib/pseo/comparisons.js` the same way (competitor, headline, metaDescription).
- `fmt:<png|jpg|webp>` — the html-to-[format] pages are separate ranking URLs sharing one registry
  entry, so each gets its own card rather than a shared one headlined "HTML to image".
- `--list tool|alt` prints the slugs, which is what the render loop iterates.

Rasterised at 1200×630 with headless Chrome, one file each:

| Where | Count |
|---|---|
| `static/og/tools/<slug>.png` | 18 registry tools + 3 format pages = 21 |
| `static/og/alternatives/<slug>.png` | 38 (was 35 old-brand; 3 pages had none) |
| `static/og/v2/blog.png` | 1, the branded fallback |

Deleted: `static/og/alternatives/hcti-io.png` and `index.png`, orphans with no route behind them.

## Headlines

The cards use the registry titles, not the routes' `<h1>`s. The dedicated routes' H1s are the same
words in caps ("CSV TO PDF"), but the nine use-case routes read "Generate Badge Generator",
"Generate Membership Card Generator", "Generate Email Header Generator" — a duplicated word that
should be fixed on the pages, not reprinted on a share card. `HEADLINES` in the script is the
per-slug override if a card should ever differ.

## Routes

Every hardcoded `media.pictify.io/<hash>.png` page card is now a `https://pictify.io/og/...` path, and
`twitter:image` equals `og:image` everywhere. Files touched: html-to-[format] (per format),
url-to-image-generator, og-image-generator + [platform] (including the `screenshot` ImageObject in its
structured data), code-to-image, csv-to-pdf, certificate-generator, online-invoice-generator,
[usecase] (falls back to `/og/tools/<useCaseId>.png`), integrations/[slug] (uses the integrations
card), and `use-cases.js` (barcode-generator's override).

Left alone deliberately, both sample images rather than page cards: `src/routes/dev/tool-states`
(`SAMPLE`) and `src/lib/components/dashboard/tools/OgImageGenerator.svelte`.

## Blogs — the user's to regenerate

Post images come from Sanity and are per-post uploads, so they are not touched here. The share card
now falls back to `/og/v2/blog.png` when a post has no image; the on-page hero plate still renders
nothing, so unillustrated posts do not all grow the same picture.

All 20 published posts currently carry an image, and none of them is in the new brand:

**On `media.pictify.io` (8):**

- `best-image-generation-apis-ranked-2026`
- `html-to-image-the-complete-developer-guide-2026`
- `html-to-gif-how-to-create-animated-images-from-code`
- `automated-image-generation-the-developers-guide-2026`
- `bannerbear-alternatives-for-developers-12-tools-compared-honestly-2026`
- `canva-alternatives-for-developers-api-first-editor-first-and-when-to-pick-each-2026`
- `handlebars-templates-the-practical-guide-if-else-each-helpers`
- `website-screenshot-api-the-complete-developer-guide-2026`

**On `res.cloudinary.com` (10):**

- `how-to-create-image-from-url`
- `why-visual-content-matters-in-online-learning`
- `building-a-quote-image-generator-for-instagram`
- `building-a-twitter-card-generator-with-node-js`
- `how-to-create-certificates-from-html-templates`
- `how-to-create-a-thumbnail-generator-using-node-js`
- `driving-user-generated-content-ugc-a-comprehensive-guide`
- `how-to-create-real-time-snapshot-of-your-reports-using-python-and-pictify`
- `optimizing-images-for-seo-a-step-by-step-guide-to-improve-your-websites-performance`
- `why-you-should-use-custom-and-personalized-email-banners`

**On `assets.seobotai.com` (2) — imported posts:**

- `10-best-html-to-image-api-use-cases-for-developers`
- `how-to-generate-social-media-cards-using-html-to-image-api`

Regenerating those means replacing the image on each document in Sanity; nothing in this repo decides them.

## Verified

og:image and twitter:image fetched from the running site for all 18 tool pages, the 3 format pages, 5
alternatives, `/`, `/pricing`, `/tools`, `/alternatives`, `/integrations` and `/integrations/n8n`: every
one resolves to `pictify.io`, and og equals twitter on all of them. Three PNGs opened and checked at
1200×630 and at 600 wide; the longest headline in the set (`playwright-self-hosted`, 60 characters)
sets to three lines and clears the glyph.
