// Shared project identity for sanity.config.js and sanity.cli.js — keep in
// sync with the frontend's PUBLIC_SANITY_PROJECT_ID / PUBLIC_SANITY_DATASET.
export const DEFAULT_PROJECT_ID = 'ayq6mmxw';
export const DEFAULT_DATASET = 'production';
